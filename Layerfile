FROM vm/ubuntu:18.0

RUN curl -sL https://deb.nodesource.com/setu16.x | sudo -E bash -
RUN sudo apt-get install -y nodejs
RUN sudo npm install npm@latest -g


COPY . .
RUN npm install
RUN BACKGROUND node index.js
EXPOSE WEBSITE http://localhost:3000
